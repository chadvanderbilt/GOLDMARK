# Core dependencies (smoke-test + CPU execution).
#
# Install optional extras when working with real WSIs / canonical foundation models:
# - Whole-slide images:   pip install -r requirements-wsi.txt
# - Canonical encoders:   pip install -r requirements-encoders.txt

numpy
pandas
requests
scikit-learn
torch
torchvision
tqdm
Pillow
